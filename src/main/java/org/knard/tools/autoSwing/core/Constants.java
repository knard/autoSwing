package org.knard.tools.autoSwing.core;

/**
 * contain all constants use in this project.
 * 
 * @author knard
 *
 */
public class Constants {

	/**
	 * delay in millisecond between two gui action. this delay is Used to avoid
	 * the application to simulate two user action to fast. In some case it can
	 * be lead that the user action will happen before the gui as react.
	 */
	public static final long DEFAULT_GUI_DELAY = 300;
}
